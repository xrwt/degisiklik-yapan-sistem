name: sistem

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        node: [18.x]
    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4
      - name: 🔩 Setup PNPM
        uses: pnpm/action-setup@v2
        with:
          version: latest
      - name: 🔩 Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
          cache: "pnpm"
      - name: 🚀 Install dependencies
        run: pnpm install
      - name: 🔄 1.000.000 icon güncelle
        run: |
          git config --global user.name "xrwt"
          git config --global user.email "xrqu.live@gmail.com"

          add_count=1
          remove_count=1
          change_count=1

          i=1
          while [ $i -le 1000000 ]; do
            echo "güncelleme #$i"

            # Generate first icon
            pnpm app:generate --icon-$i

            # Check if there are changes to commit for addition
            if [ -n "$(git status --porcelain)" ]; then
              git add .
              git commit -m "[➕] Güncelleme ($add_count. Ekleme, $change_count. Değişiklik)"
              add_count=$((add_count + 1))
            fi

            # Push changes for addition
            git push origin ${{ github.head_ref }}

            # Remove first icon
            pnpm app:remove --icon-$i

            # Check if there are changes to commit for removal
            if [ -n "$(git status --porcelain)" ]; then
              git add .
              git commit -m "[➖] Güncelleme ($remove_count. Çıkarma, $change_count. Değişiklik)"
              remove_count=$((remove_count + 1))
            fi

            # Push changes for removal
            git push origin ${{ github.head_ref }}

            # Increment change count after each complete cycle of addition and removal
            change_count=1
            i=$((i + 1))
          done
