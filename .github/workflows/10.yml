name: sistem

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        node: [18.x]
    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4
      - name: 🔩 Setup PNPM
        uses: pnpm/action-setup@v2
        with:
          version: latest
      - name: 🔩 Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
          cache: "pnpm"
      - name: 🚀 Install dependencies
        run: pnpm install
      - name: 🔄 1.000.000 icon güncelle
        run: |
          git config --global user.name "xrwt"
          git config --global user.email "xrqu.live@gmail.com"

          for i in {1..1000000}; do
            echo "güncelleme #$i"
            
            # Remove first icon
            pnpm app:remove --icon-$i
            git add .
            git commit -m "[➖] Güncelleme ($i. Değişiklik)"
            
            # Remove second icon
            pnpm app:remove --icon-$((i+1))
            git add .
            git commit -m "[➖] Güncelleme ($((i+1)). Değişiklik)"
            
            # Generate first icon
            pnpm app:generate --icon-$i
            git add .
            git commit -m "[➕] Güncelleme ($i. Değişiklik)"
            
            # Generate second icon
            pnpm app:generate --icon-$((i+1))
            git add .
            git commit -m "[➕] Güncelleme ($((i+1)). Değişiklik)"
            
            # Push the changes to the repository
            git push origin ${{ github.head_ref }}
            
            # Increment the loop counter by 2
            i=$((i+1))
          done
